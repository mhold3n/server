# Monorepo: multiple services use a top-level package directory named `src`.
# Without these flags, mypy reports "Duplicate module named src" when checking
# more than one service in a single invocation.
[mypy]
python_version = 3.11
namespace_packages = True
explicit_package_bases = True
