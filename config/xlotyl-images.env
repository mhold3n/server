# Pin the published AI stack from the xlotyl repo (see xlotyl/.github/workflows/publish-images.yml).
# Images are ghcr.io/<github_repo_owner>/<service>; override prefix after org transfer (e.g. ghcr.io/xlotyl).
XLOTYL_IMAGE_PREFIX=ghcr.io/mhold3n
XLOTYL_VERSION=0.1.0
