version: "3.8"
services:
  runtime:
    build: .
    volumes:
      - ./volumes/runtime-bundles:/app/outputs/packaged/runtime-bundles
      - ./volumes/sqlite:/app/db
    ports:
      - "8000:8000"
    environment:
      - RUNTIME_MODE=openai_compatible
