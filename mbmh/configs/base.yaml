seed: 42
output_dir: "outputs/runs"
logging_dir: "logs"
max_steps: 1000
save_strategy: "steps"
eval_strategy: "steps"
gradient_checkpointing: true
fp16: false
bf16: false
report_to: "none"
task_type: "base"
strict_validation: true
