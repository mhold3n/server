# Repo-wide Ruff settings. WrkHrs AI stack lives under `services/wrkhrs`; targeted
# ruff runs in CI still cover gateway/orchestrator and key tests (see ci.yml).
line-length = 88
target-version = "py311"
